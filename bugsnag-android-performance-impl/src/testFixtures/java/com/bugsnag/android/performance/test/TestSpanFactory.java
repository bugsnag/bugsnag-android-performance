package com.bugsnag.android.performance.test;

/**
 * Legacy Java stub retained only so the old file path remains present.
 * The active test-fixtures implementation lives in `TestSpanFactory.kt`.
 */
final class TestSpanFactoryJavaStub {}
