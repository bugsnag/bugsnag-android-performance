package com.bugsnag.android.performance.test

/**
 * Retained only so the source file path exists; the actual helper is provided by
 * `TestTimeoutExecutor.java`.
 */
internal class TestTimeoutExecutorKtStub

